services:
  remote-render:
    build: .
    ports:
      - "${REMOTE_RENDER_PORT:-8080}:8080"
    environment:
      PYTHONUNBUFFERED: "1"
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"
