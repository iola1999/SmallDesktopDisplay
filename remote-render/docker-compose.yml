services:
  remote-render:
    build: .
    restart: unless-stopped
    ports:
      - "${REMOTE_RENDER_PORT:-8080}:8080"
    environment:
      NODE_ENV: production
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"
